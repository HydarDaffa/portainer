name: portainer-apps
version: 1.0.0
description: Portainer CE Deployment
container:
  image: portainer/portainer-ce:latest
  port: 9000   # bisa juga 9443
resources:
  cpu: 500m
  memory: 512Mi
  replicas: 1
volumes:
  - name: portainer-data
    persistent: true
domain: portainer.deployaja.id
envMap: {}
